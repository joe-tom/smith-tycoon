# Plan 5: 엔딩 시스템 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 6종 엔딩(승리 2 / 패배 4)을 전투 직후·100일 도달 시점에 판정하고 GameOver UI로 표시한다.

**Architecture:** `players` 테이블에 누적 카운터(`heroes_died_total`, `weapons_destroyed_total`) + `ending_kind` 컬럼 추가. 새 모듈 `app/endgame.py`가 두 종류의 detector(`detect_post_battle`, `detect_day_100`) + 적용 함수 제공. `combat.run_battle` 끝에 post_battle 검사, `api/day.py:post_next_day` 진입 시 day 100 검사. UI는 ending_kind로 `frontend/src/endings.ts`의 메타를 조회해 GameOver 컴포넌트가 렌더.

**Tech Stack:** FastAPI, Supabase Postgres (마이그레이션 008), Pytest, React/Vite.

**Spec:** `docs/superpowers/specs/2026-05-26-endgame-design.md`

---

## Phase 0 — 준비

### Task 0.1: DB 마이그레이션 008

**Files:** Create `backend/migrations/008_endgame.sql`

- [ ] **Step 1: SQL 파일 생성**

```sql
-- 008_endgame.sql — 엔딩 시스템: 누적 카운터 + ending_kind
alter table players
  add column heroes_died_total       int  not null default 0,
  add column weapons_destroyed_total int  not null default 0,
  add column ending_kind             text;
```

- [ ] **Step 2: Supabase MCP로 적용**

`mcp__plugin_supabase_supabase__apply_migration` 호출:
- project_id: `lgxjxkiyychicfzwbirp`
- name: `008_endgame`
- query: 위 SQL

- [ ] **Step 3: 검증**

`mcp__plugin_supabase_supabase__execute_sql`로:
```sql
select column_name, data_type, column_default from information_schema.columns
where table_name='players' and column_name in ('heroes_died_total','weapons_destroyed_total','ending_kind')
order by column_name;
```
Expected: 3 rows. `heroes_died_total`/`weapons_destroyed_total` → `integer`, default `0`. `ending_kind` → `text`, default `null`.

- [ ] **Step 4: Commit**

```bash
cd /home/afraidnot/dev/smith-tycoon && git add backend/migrations/008_endgame.sql && git commit -m "feat(db): 008 — endgame counters + ending_kind

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

### Task 0.2: `repo.get_or_create_player_by_nickname` 신규 row에 카운터 초기화

**Files:** Modify `backend/app/repo.py` (`get_or_create_player_by_nickname`)

DB default가 0/null이라 INSERT 시 명시 안 해도 됨. 하지만 명시적으로 박아두면 FakeRepo·테스트와 일관성. 본 task는 선택적이지만 권장.

- [ ] **Step 1: INSERT payload 수정**

`backend/app/repo.py`의 `get_or_create_player_by_nickname` 내부 `c.table("players").insert({...})` 호출에서 dict에 다음 키 추가:

```python
"heroes_died_total": 0,
"weapons_destroyed_total": 0,
"ending_kind": None,
```

- [ ] **Step 2: 검증**

```bash
cd /home/afraidnot/dev/smith-tycoon/backend && source .venv/bin/activate && python -m pytest -q
```
Expected: 변경 없음 (기존 테스트 모두 통과).

- [ ] **Step 3: Commit**

```bash
cd /home/afraidnot/dev/smith-tycoon && git add backend/app/repo.py && git commit -m "chore(repo): seed endgame counters on player create

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Phase 1 — endgame 모듈 (TDD)

### Task 1.1: `endgame.py` + tests

**Files:**
- Create `backend/app/endgame.py`
- Create `backend/tests/test_endgame.py`

- [ ] **Step 1: 실패 테스트**

Create `backend/tests/test_endgame.py`:

```python
from app.endgame import detect_post_battle, detect_day_100, ENDINGS


def test_endings_have_all_required_fields():
    ids = {e["id"] for e in ENDINGS}
    assert ids == {"surt_killed", "lonely_demon", "forge_burns",
                   "retirement", "youth_blood", "weapons_broken"}
    for e in ENDINGS:
        assert "title" in e and "won" in e and "flavor" in e


def test_post_battle_surt_killed_highest_priority():
    p = {"heroes_died_total": 999, "weapons_destroyed_total": 999}
    assert detect_post_battle(p, {"surt"}) == "surt_killed"


def test_post_battle_youth_blood():
    p = {"heroes_died_total": 200, "weapons_destroyed_total": 0}
    assert detect_post_battle(p, set()) == "youth_blood"


def test_post_battle_weapons_broken():
    p = {"heroes_died_total": 0, "weapons_destroyed_total": 200}
    assert detect_post_battle(p, set()) == "weapons_broken"


def test_post_battle_youth_before_weapons():
    p = {"heroes_died_total": 200, "weapons_destroyed_total": 200}
    assert detect_post_battle(p, set()) == "youth_blood"


def test_post_battle_no_trigger():
    p = {"heroes_died_total": 199, "weapons_destroyed_total": 199}
    assert detect_post_battle(p, set()) is None


def test_day_100_with_surt_dead_returns_none():
    # surt 이미 죽었으면 surt_killed가 post_battle에서 처리됨
    assert detect_day_100({}, {"surt"}) is None


def test_day_100_lonely_demon():
    mids = {"belphegor","beelzebub","mammon","leviathan","asmodeus","satan","lucifer"}
    assert detect_day_100({}, mids) == "lonely_demon"


def test_day_100_forge_burns_partial():
    assert detect_day_100({}, {"belphegor","beelzebub","mammon"}) == "forge_burns"


def test_day_100_retirement_zero_mid():
    assert detect_day_100({}, set()) == "retirement"


def test_day_100_forge_burns_single_kill():
    assert detect_day_100({}, {"belphegor"}) == "forge_burns"
```

- [ ] **Step 2: 실패 확인**

```bash
cd /home/afraidnot/dev/smith-tycoon/backend && source .venv/bin/activate && python -m pytest tests/test_endgame.py -v
```
Expected: ModuleNotFoundError (`app.endgame` 없음).

- [ ] **Step 3: 구현 — `backend/app/endgame.py`**

```python
"""엔딩 판정 로직 + 적용 함수."""
from __future__ import annotations
from typing import Any
from . import repo


MID_BOSS_IDS: set[str] = {
    "belphegor", "beelzebub", "mammon", "leviathan",
    "asmodeus", "satan", "lucifer",
}


ENDINGS: list[dict[str, Any]] = [
    {"id": "surt_killed", "title": "🏆 마왕 토벌", "won": True,
     "flavor": "수르트의 화염이 사그라들었다. 7대 죄악도 함께 무너졌고, 인간 세계는 다시 빛을 찾았다. 당신의 망치는 전설이 되었다."},
    {"id": "lonely_demon", "title": "🌒 외로운 마왕", "won": True,
     "flavor": "7대 죄악은 모두 무너졌지만 수르트는 끝내 모습을 드러내지 않았다. 100일의 항전은 끝나고, 세상은 마왕 하나만 남긴 채 평온해졌다."},
    {"id": "forge_burns", "title": "🔥 다 쓰러져가는 대장간은 불타야 해", "won": False,
     "flavor": "100일이 지났지만 수르트는 건재하다. 절반의 죄악을 베어낸 당신의 무기들은 영광스럽지만, 정작 마왕은 닿지 못한 곳에 있다. 대장간 문을 닫을 시간이다."},
    {"id": "retirement", "title": "💤 정년 퇴직", "won": False,
     "flavor": "100일 동안 망치질만 했다. 단 한 명의 죄악도 무너뜨리지 못했고 수르트는 더더욱. 당신은 평범한 대장장이로 늙어간다."},
    {"id": "youth_blood", "title": "💀 이기지도 못할 거면서 왜 싸웠어?", "won": False,
     "flavor": "200명의 용사가 당신 손에서 무기를 받았고, 200명이 돌아오지 못했다. 마을 입구마다 곡소리가 그치지 않는다."},
    {"id": "weapons_broken", "title": "⚔️ 우리나라 청년들은 너 때문에 죽은 거야", "won": False,
     "flavor": "당신이 만든 무기 200개가 마왕군 앞에서 부러졌다. 살아 돌아온 용사들의 손에는 부러진 자루만 남았고, 그들의 분노는 당신을 향한다."},
]


def detect_post_battle(player: dict[str, Any], defeated_boss_ids: set[str]) -> str | None:
    """전투 직후 검사. 우선순위: surt > heroes_died ≥200 > weapons_destroyed ≥200."""
    if "surt" in defeated_boss_ids:
        return "surt_killed"
    if int(player.get("heroes_died_total", 0)) >= 200:
        return "youth_blood"
    if int(player.get("weapons_destroyed_total", 0)) >= 200:
        return "weapons_broken"
    return None


def detect_day_100(player: dict[str, Any], defeated_boss_ids: set[str]) -> str | None:
    """day 100 도달 시 검사. surt 이미 처치돼 있으면 None (post_battle 경로로 끝남)."""
    if "surt" in defeated_boss_ids:
        return None
    mid_dead = len(defeated_boss_ids & MID_BOSS_IDS)
    if mid_dead == 7:
        return "lonely_demon"
    if mid_dead >= 1:
        return "forge_burns"
    return "retirement"


def apply_ending(player_id: int, ending_id: str) -> None:
    """player row에 ending_kind 기록 + phase='game_over' 전이."""
    repo.update_player(player_id, ending_kind=ending_id, current_phase="game_over")
```

- [ ] **Step 4: 통과 확인**

```bash
python -m pytest tests/test_endgame.py -v
```
Expected: 11 passed.

- [ ] **Step 5: Commit**

```bash
cd /home/afraidnot/dev/smith-tycoon && git add backend/app/endgame.py backend/tests/test_endgame.py && git commit -m "feat(endgame): 6 endings + detector functions

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Phase 2 — `combat.run_battle` 통합

### Task 2.1: 누적 카운터 증가 + post-battle ending 체크

**Files:**
- Modify `backend/app/combat.py` (`run_battle`)
- Modify `backend/tests/test_integration_day.py` (FakeRepo player dict)
- Modify `backend/tests/test_integration_meta.py` (FakeRepo player dict)

- [ ] **Step 1: FakeRepo player dict에 카운터 필드 추가 (두 파일)**

`backend/tests/test_integration_day.py`와 `backend/tests/test_integration_meta.py`의 `FakeRepo.__init__`에 있는:

```python
self.player = {"id": 1, "gold": 5000, "reputation": 0, "current_day": 1,
               "current_phase": "forge_open", "craft_power": 0}
```

(또는 메타 파일에는 약간 변형된 dict가 있을 수 있음. 그 dict에)

다음 3개 키 추가:

```python
"heroes_died_total": 0, "weapons_destroyed_total": 0, "ending_kind": None,
```

각 파일에 한 번씩.

- [ ] **Step 2: `run_battle` 수정 — `backend/app/combat.py`**

상단 import 추가:
```python
from . import endgame
```

`run_battle` 내부, 현재 `delta = apply_outcomes(outcomes)` 직후의 보스 보너스 블록 (`if outcomes["demon"] == "killed" and demon.get("is_boss"): delta["reputation"] += 10`) 다음에 카운터 계산 추가:

```python
extra: dict[str, Any] = {}
if outcomes["hero"] == "died":
    extra["heroes_died_total"] = int(player.get("heroes_died_total", 0)) + 1
if outcomes["weapon"] == "destroyed":
    extra["weapons_destroyed_total"] = int(player.get("weapons_destroyed_total", 0)) + 1
```

그 다음의 `repo.update_player(pid, reputation=..., current_phase=...)` 호출에 `**extra` 추가:

```python
repo.update_player(pid,
                   reputation=player["reputation"] + delta["reputation"],
                   current_phase=state_machine.next_phase(player["current_phase"]),
                   **extra)
```

함수 후반부, 현재 `battle_row = repo.insert_battle(...)` 와 `repo.insert_day_event(...)` (kind="battle") 와 보스 처리 (boss_kill / surt_kill 이벤트 기록) 모두 끝난 후, 가장 마지막 return 직전에 endgame 체크 추가:

```python
post_player = repo.load_player(pid)
ending = endgame.detect_post_battle(
    post_player, repo.list_defeated_boss_ids(pid)
)
if ending:
    endgame.apply_ending(pid, ending)

return {"script": script, "outcomes": outcomes, "demon": demon,
        "next_phase": repo.load_player(pid)["current_phase"]}
```

(이미 마지막 줄이 `repo.load_player(pid)["current_phase"]`로 phase를 다시 읽고 있다면 그대로. 만약 캐시된 변수를 쓰고 있다면 다시 읽도록 바꿔야 함 — apply_ending이 phase를 game_over로 바꿨을 수 있으니.)

- [ ] **Step 3: 통과 확인 — 기존 테스트 회귀 없음**

```bash
cd /home/afraidnot/dev/smith-tycoon/backend && source .venv/bin/activate && python -m pytest -q
```
Expected: all pass (기존 통합 테스트는 day 1·짧은 전투라 200 카운터 도달 없음).

- [ ] **Step 4: Commit**

```bash
cd /home/afraidnot/dev/smith-tycoon && git add backend/app/combat.py backend/tests/test_integration_day.py backend/tests/test_integration_meta.py && git commit -m "feat(combat): increment counters + post-battle ending check

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

### Task 2.2: Surt 처치 통합 테스트 (ending 적용 확인)

**Files:** Modify `backend/tests/test_integration_meta.py`

기존 `test_day_100_surt_appears_and_logs_kill_events`가 있음. 그 테스트가 surt 처치 시 ending도 잡히는지 추가 확인하도록 확장.

- [ ] **Step 1: 기존 테스트 본문 끝에 추가 검증**

`backend/tests/test_integration_meta.py`의 `test_day_100_surt_appears_and_logs_kill_events` 내부, `if result["outcomes"]["demon"] == "killed":` 블록 안에 추가:

```python
        # 엔딩 적용 확인
        assert fake.player["ending_kind"] == "surt_killed"
        assert fake.player["current_phase"] == "game_over"
```

- [ ] **Step 2: 통과 확인**

```bash
python -m pytest tests/test_integration_meta.py::test_day_100_surt_appears_and_logs_kill_events -v
```
Expected: PASS (단, run_battle이 결정론이 아니라 시드 운에 따라 'killed' 안 나올 수도 있음. 그 경우 ending 검증 부분이 if 안에 있어서 skip되니 테스트는 그래도 PASS).

- [ ] **Step 3: Commit**

```bash
cd /home/afraidnot/dev/smith-tycoon && git add backend/tests/test_integration_meta.py && git commit -m "test: surt kill triggers surt_killed ending

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Phase 3 — day 100 ending 체크

### Task 3.1: `state_machine.MAX_DAY` 100으로 변경

**Files:** Modify `backend/app/state_machine.py`

- [ ] **Step 1: 변경**

`backend/app/state_machine.py`의 `MAX_DAY = 5`를:

```python
MAX_DAY = 100
```

- [ ] **Step 2: 회귀 확인**

```bash
cd /home/afraidnot/dev/smith-tycoon/backend && source .venv/bin/activate && python -m pytest -q
```
Expected: all pass. (state_machine 테스트가 MAX_DAY=5에 의존하는지 확인. 만약 있으면 100으로 업데이트.)

- [ ] **Step 3: Commit**

```bash
cd /home/afraidnot/dev/smith-tycoon && git add backend/app/state_machine.py backend/tests/test_state_machine.py 2>/dev/null || git add backend/app/state_machine.py && git commit -m "chore: extend game length from 5 to 100 days

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

### Task 3.2: `api/day.py:post_next_day` day 100 ending 체크

**Files:** Modify `backend/app/api/day.py`

- [ ] **Step 1: 함수 교체**

`backend/app/api/day.py`의 `post_next_day` 전체를 다음으로 교체:

```python
@router.post("/day/next")
def post_next_day(player: dict = Depends(current_player)):
    if player["current_phase"] != "day_summary":
        raise HTTPException(400, detail={"error": "wrong_phase",
                                          "current_phase": player["current_phase"]})
    # day 100 도달 시 엔딩 검사 (수르트는 전투 직후 경로로 끝났을 것)
    if player["current_day"] == 100:
        from .. import endgame
        defeated = repo.list_defeated_boss_ids(player["id"])
        ending = endgame.detect_day_100(player, defeated)
        if ending:
            endgame.apply_ending(player["id"], ending)
            return {"ok": True, "ending": ending,
                    "current_day": 100, "current_phase": "game_over"}
    state_machine.advance_to_next_day(player)
    repo.update_player(player["id"], current_day=player["current_day"],
                       current_phase=player["current_phase"])
    return {"ok": True, "current_day": player["current_day"],
            "current_phase": player["current_phase"]}
```

- [ ] **Step 2: 통과 확인**

```bash
python -m pytest -q
```
Expected: all pass.

- [ ] **Step 3: Commit**

```bash
cd /home/afraidnot/dev/smith-tycoon && git add backend/app/api/day.py && git commit -m "feat(api/day): day 100 ending check before advance

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Phase 4 — `/state` 응답 확장

### Task 4.1: `api/state.py`에 boss_kill_count 추가

**Files:** Modify `backend/app/api/state.py`

- [ ] **Step 1: 응답 dict에 필드 추가**

`backend/app/api/state.py`의 `get_state` 함수 마지막 return문 직전에:

```python
boss_kill_count = len(repo.list_defeated_boss_ids(player["id"]))
```

그리고 return dict에 `"boss_kill_count": boss_kill_count` 추가.

전체 return 모양:
```python
return {
    "player": player,
    "inventory": inventory,
    "weapons": weapons,
    "hero": hero,
    "merchant": merchant_today,
    "boss_kill_count": boss_kill_count,
}
```

- [ ] **Step 2: 검증**

```bash
python -m pytest -q
```
Expected: all pass.

- [ ] **Step 3: Commit**

```bash
cd /home/afraidnot/dev/smith-tycoon && git add backend/app/api/state.py && git commit -m "feat(api/state): expose boss_kill_count

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Phase 5 — 프론트엔드

### Task 5.1: 타입 확장 + endings.ts

**Files:**
- Modify `frontend/src/types.ts`
- Create `frontend/src/endings.ts`

- [ ] **Step 1: 타입 추가 — `frontend/src/types.ts`**

`Player` 인터페이스 끝에 세 필드 추가:
```typescript
  heroes_died_total: number;
  weapons_destroyed_total: number;
  ending_kind: string | null;
```

`StateResponse` 인터페이스에 필드 추가:
```typescript
  boss_kill_count: number;
```

- [ ] **Step 2: `frontend/src/endings.ts` 생성**

```typescript
export const ENDINGS: Record<string, { title: string; won: boolean; flavor: string }> = {
  surt_killed: {
    title: "🏆 마왕 토벌", won: true,
    flavor: "수르트의 화염이 사그라들었다. 7대 죄악도 함께 무너졌고, 인간 세계는 다시 빛을 찾았다. 당신의 망치는 전설이 되었다.",
  },
  lonely_demon: {
    title: "🌒 외로운 마왕", won: true,
    flavor: "7대 죄악은 모두 무너졌지만 수르트는 끝내 모습을 드러내지 않았다. 100일의 항전은 끝나고, 세상은 마왕 하나만 남긴 채 평온해졌다.",
  },
  forge_burns: {
    title: "🔥 다 쓰러져가는 대장간은 불타야 해", won: false,
    flavor: "100일이 지났지만 수르트는 건재하다. 절반의 죄악을 베어낸 당신의 무기들은 영광스럽지만, 정작 마왕은 닿지 못한 곳에 있다. 대장간 문을 닫을 시간이다.",
  },
  retirement: {
    title: "💤 정년 퇴직", won: false,
    flavor: "100일 동안 망치질만 했다. 단 한 명의 죄악도 무너뜨리지 못했고 수르트는 더더욱. 당신은 평범한 대장장이로 늙어간다.",
  },
  youth_blood: {
    title: "💀 이기지도 못할 거면서 왜 싸웠어?", won: false,
    flavor: "200명의 용사가 당신 손에서 무기를 받았고, 200명이 돌아오지 못했다. 마을 입구마다 곡소리가 그치지 않는다.",
  },
  weapons_broken: {
    title: "⚔️ 우리나라 청년들은 너 때문에 죽은 거야", won: false,
    flavor: "당신이 만든 무기 200개가 마왕군 앞에서 부러졌다. 살아 돌아온 용사들의 손에는 부러진 자루만 남았고, 그들의 분노는 당신을 향한다.",
  },
};
```

- [ ] **Step 3: tsc 확인**

```bash
cd /home/afraidnot/dev/smith-tycoon/frontend && npx tsc --noEmit; echo "tsc=$?"
```
Expected: `tsc=0`.

- [ ] **Step 4: Commit**

```bash
cd /home/afraidnot/dev/smith-tycoon && git add frontend/src/types.ts frontend/src/endings.ts && git commit -m "feat(frontend): Player counters + ending types + ENDINGS metadata

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

### Task 5.2: GameOver 컴포넌트 재작성

**Files:** Modify `frontend/src/components/GameOver.tsx`

- [ ] **Step 1: 전체 교체**

기존 `GameOver.tsx` 내용을 다음으로 완전히 교체:

```tsx
import { useState } from "react";
import { api } from "../api";
import type { StateResponse } from "../types";
import { ENDINGS } from "../endings";

export function GameOver({ state, onReset }: { state: StateResponse; onReset: () => void }) {
  const [busy, setBusy] = useState(false);
  const reset = async () => {
    setBusy(true);
    try { await api.resetGame(); onReset(); }
    finally { setBusy(false); }
  };
  if (!state.player) return null;
  const kind = state.player.ending_kind;
  const meta = kind ? ENDINGS[kind] : null;
  return (
    <div style={{ padding: 24 }}>
      <h2>{meta?.title ?? "게임 종료"}</h2>
      <p style={{ whiteSpace: "pre-wrap", color: meta?.won ? "#080" : "#a30" }}>
        {meta?.flavor ?? "100일 운영이 종료되었습니다."}
      </p>
      <div style={{ marginTop: 16, padding: 12, background: "#f5f5f5" }}>
        <p>Day {state.player.current_day} / 100</p>
        <p>골드 {state.player.gold.toLocaleString()} · 평판 {state.player.reputation}</p>
        <p>처치한 보스: {state.boss_kill_count}명</p>
        <p>사망한 용사: {state.player.heroes_died_total}명</p>
        <p>파괴된 무기: {state.player.weapons_destroyed_total}개</p>
      </div>
      <button className="btn" onClick={reset} disabled={busy} style={{ marginTop: 16 }}>
        새 게임
      </button>
    </div>
  );
}
```

- [ ] **Step 2: tsc 확인**

```bash
npx tsc --noEmit; echo "tsc=$?"
```
Expected: `tsc=0` (DayRouter가 아직 state prop을 전달하지 않으면 컴파일 에러 나옴. 그 경우 Task 5.3에서 동시에 고침. 만약 컴파일 에러면 5.3을 먼저 끝낸 뒤 다시 확인.)

- [ ] **Step 3: Commit**

```bash
cd /home/afraidnot/dev/smith-tycoon && git add frontend/src/components/GameOver.tsx && git commit -m "feat(frontend): rewrite GameOver with ending metadata + stats

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

### Task 5.3: DayRouter에 state prop 전달

**Files:** Modify `frontend/src/components/DayRouter.tsx`

- [ ] **Step 1: GameOver 호출 수정**

`frontend/src/components/DayRouter.tsx` 안에서 `<GameOver onReset={onReset} />` 라인을:

```tsx
return <GameOver state={state} onReset={onReset} />;
```

으로 변경.

- [ ] **Step 2: tsc 확인**

```bash
cd /home/afraidnot/dev/smith-tycoon/frontend && npx tsc --noEmit; echo "tsc=$?"
```
Expected: `tsc=0`.

- [ ] **Step 3: Commit**

```bash
cd /home/afraidnot/dev/smith-tycoon && git add frontend/src/components/DayRouter.tsx && git commit -m "feat(frontend): pass state to GameOver

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Phase 6 — 최종 검증

### Task 6.1: 전체 테스트 + tsc + 라이브 smoke

- [ ] **Step 1: 백엔드 풀 테스트**

```bash
cd /home/afraidnot/dev/smith-tycoon/backend && source .venv/bin/activate && python -m pytest -q
```
Expected: 모두 PASS (기존 133 + 신규 ~11 ≈ 144+).

- [ ] **Step 2: 프론트엔드 tsc**

```bash
cd /home/afraidnot/dev/smith-tycoon/frontend && npx tsc --noEmit; echo "tsc=$?"
```
Expected: `tsc=0`.

- [ ] **Step 3: 라이브 smoke (수르트 처치 시나리오)**

uvicorn으로 백엔드 띄움 → 새 닉네임으로 `/state`로 player 생성 → Supabase로 player_id 알아내 day=100, phase='hero1_battle'로 강제 + 강한 무기·용사 삽입 → `POST /battle` → 응답에서 `next_phase='game_over'` 확인 → `GET /state`로 `player.ending_kind` 값 확인.

샘플 명령 (포트 8768 가정):
```bash
cd /home/afraidnot/dev/smith-tycoon/backend && source .venv/bin/activate && uvicorn app.main:app --port 8768 &
sleep 2
curl -s -H "X-Player-Nickname: ENDTEST" http://127.0.0.1:8768/state | head -c 100
# Supabase MCP로 day=100, phase='hero1_battle' 강제 + 무기·용사·hero_roster 삽입
# (Task 4.3 라이브 smoke 절차 재사용)
curl -s -X POST -H "X-Player-Nickname: ENDTEST" http://127.0.0.1:8768/battle
# 결과에 next_phase=game_over 확인
curl -s -H "X-Player-Nickname: ENDTEST" http://127.0.0.1:8768/state
# player.ending_kind 가 "surt_killed" 인지 확인
pkill -f "uvicorn app.main:app --port 8768"
```

- [ ] **Step 4: 발견된 버그 fix 커밋 (있으면)**

---

## Self-review 메모

- 스펙 §2 엔딩 정의 — Task 1.1로 커버.
- 스펙 §3 DB 마이그레이션 — Task 0.1로 커버.
- 스펙 §4.1 endgame.py — Task 1.1로 커버.
- 스펙 §4.2 combat.run_battle — Task 2.1로 커버.
- 스펙 §4.3 day/next ending check — Task 3.2로 커버.
- 스펙 §4.4 state boss_kill_count — Task 4.1로 커버.
- 스펙 §5 프론트엔드 — Task 5.1, 5.2, 5.3으로 커버.
- 스펙 §6 테스트 — Task 1.1, 2.2로 커버.
- 추가 발견: `state_machine.MAX_DAY=5`가 100일 게임과 안 맞음 → Task 3.1로 100 변경.
- 스펙 §7 파일 구조 — 위 Task 매핑과 일치.
- 스펙 §8 범위 외 (나머지 4개 엔딩, LLM 플레이버, 명예의 전당) — 본 플랜에서 의도적 미포함.
